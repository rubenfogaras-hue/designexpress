Put your two hero photos here:

  before.jpg   → the room "before" (top image)
  after.jpg    → the redesigned room "after" (bottom image)

Until they exist, the page shows a warm placeholder block instead of a broken
image. As soon as you add files with these exact names, they appear on the page
automatically — no code change needed. JPG or PNG both work (keep the .jpg name).
